#' @param MS_custom_name_vec A vector of function names to use if the management 
#'  strategy (MS) option 'custom' is used. If no scenarios with MS = 'custom' 
#'  are used, then  MS_custom_name_vec will not be used and can be set as NULL.
#'  If some scenarios use MS = 'custom' and some use a different management
#'  strategy, the scenarios not using custom can use NA in MS_custom_name_vec.
